<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TraveLink" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/red_dark</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/blue</item>
        <item name="colorSecondaryVariant">@color/blue_dark</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->

        <item name="android:windowBackground">@color/white</item>
        <item name="android:textColor">@color/black</item>
        <item name="colorProfileImage">@color/grease</item>

        <!-- Font family for all app -->
        <item name="android:fontFamily">@font/roboto_regular</item>

        <!-- Colors Bottom Navigation View -->
        <item name="color_bnv_background">@color/day_color_bnv</item>
        <item name="color_bnv_checked">@color/day_color_bnv_checked</item>
        <item name="color_bnv_not_checked">@color/day_color_bnv_not_checked</item>

        <!-- Color background list -->
        <item name="colorBackgroundList">@color/list_background_day</item>

        <!-- Color separator -->
        <item name="colorSeparator">@color/black</item>

        <!-- Color EditText disabled -->
        <item name="colorEditTextDisabled">@color/edit_text_disabled_day</item>

    </style>
</resources>